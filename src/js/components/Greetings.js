import React from 'react';

const Greeting = ({}) => {
    return (
        <div className="jumbotron">
            <h1>Hi</h1>
        </div>
    );
}
//
// Greeting.propTypes = {
//     : React.PropTypes.
// };

export default Greeting;
